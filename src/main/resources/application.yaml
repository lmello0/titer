#file: noinspection YAMLSchemaValidation
spring:
  application:
    name: titer
  datasource:
    url: jdbc:sqlite:./data/app.db
    username: sa
    password: sa
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
